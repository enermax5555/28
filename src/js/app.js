import "../scss/app.scss";

window.addEventListener("DOMContentLoaded", () => {
  // This block will be executed once the page is loaded and ready
	let hot = document.querySelectorAll(".hot");
	hot[0].textContent=hot[0].textContent+"🔥";
});
